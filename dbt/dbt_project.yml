name: divvy_analytics
version: '1.0.0'
config-version: 2

profile: divvy_analytics

model-paths: ["models"]

models:
  divvy_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table